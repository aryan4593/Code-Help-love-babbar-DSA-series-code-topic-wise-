/*input restricted Queue
in these Queue, insertion is allowd only fron back
but poping is allowed from both side 

ouput restriced Queu
in this, insertion both side available
output or removal can be done only from front

doubly ended Queue
4 types of operations are allowed - push_back,push_front, pop_back, Pop_front*/